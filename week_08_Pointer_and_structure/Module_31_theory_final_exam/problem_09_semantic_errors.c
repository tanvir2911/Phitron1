
// C program to demonstrate
// a semantic error
#include <stdio.h>

int main()
{
  int x = 10;
      b = 20, c;
  x + y = c;
  printf("%d", c);
    return 0;
}
//When a sentence is syntactically correct but has no meaning,
//semantic errors occur. This is similar to grammatical errors.
//If an expression is entered on the left side of the assignment operator,
//a semantic error may occur.
