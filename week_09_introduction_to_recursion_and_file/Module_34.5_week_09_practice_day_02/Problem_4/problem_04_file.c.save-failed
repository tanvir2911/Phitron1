#include<stdio.h>

int main()
{
    int n,sum=0;
    scanf("%d",&n);
    for(int i=1;i<=n;i++)
    {
        int x;
        scanf("%d",&x);
        sum+=(x%10);
    }
    printf("Sum = %d",sum);

    return 0;
}
